<%= form_with model: [ @article, @comment ] do |form| %>
  <p>
    <%= form.label :body %><br>
    <%= form.text_area :body %>
    <%@comment.errors.full_messages_for(:body).each do |message| %>
      <div><%= message %></div>
    <% end %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>
